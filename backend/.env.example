# Copy to backend/.env and adjust. See docs/setup.md.
PORT=3000

# PostgreSQL connection (matches docker/.env defaults)
PGHOST=localhost
PGPORT=5432
PGUSER=site_admin
PGPASSWORD=change_me
PGDATABASE=site_management

# Auth
JWT_SECRET=dev_secret_change_me
JWT_EXPIRES_IN=7d
